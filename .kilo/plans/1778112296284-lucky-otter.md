# Restore Complete Header + Sidebar + Brand Assets Version

## Objective
Restore and fully integrate the version that includes:
- **Actual header** (not inline) – full `Header.tsx` with breadcrumbs, search, notifications, avatar
- **Blue sidebar** – SPATIAL themed `RoleSidebar.tsx` with gradient branding
- **Favicon** – site tab icon linking to brand identity
- **Logos & watermarks** – full SVG assets in `public/brand/`

---

## Current State Analysis

### What Exists (Uncommitted Changes)
| Component | File | Status |
|---|---|---|
| Enterprise Header | `src/components/shell/Header.tsx` | Modified (uncommitted) |
| Blue Sidebar (gradient header block) | `src/components/layout/RoleSidebar.tsx` | Modified (uncommitted) |
| Brand assets folder | `public/brand/` (logo.svg, logo-watermark.svg) | Untracked |
| Design plan | `design-assets/SPATIAL_Brand_Identity_Implementation_Plan.md` | Untracked |
| SPATIAL tokens | `src/app/tokens.css` | Modified (uncommitted) |
| Global styles | `src/app/globals.css` | Modified (uncommitted) |

### What's Missing / Inconsistent
1. **Favicon files** – `public/favicon.ico`, `public/apple-touch-icon.png`, `public/icon-192.png` not generated
2. **Metadata** – `layout.tsx` lacks `icons` and `themeColor` in metadata
3. **Dual layout system** – Both `AppLayout.tsx` (inline header) and `AppShell.tsx` (sidebar layout) coexist; pages use `AppShell` but the actual header component is separate (`Header.tsx`)
4. **Unified integration** – Header needs to include the logo image from `public/brand/logo-icon.svg`

---

## Restoration Plan

### Phase 1 — Favicon & Logo Asset Generation
1. Convert `public/brand/logo-icon.svg` to favicon formats:
   - Generate `public/favicon.ico` (multi-size ICO)
   - Generate `public/apple-touch-icon.png` (180×180)
   - Generate `public/icon-192.png` (192×192 for PWA)
2. Place all generated icons in `public/` root

### Phase 2 — Metadata & Head Integration
Update `src/app/[locale]/layout.tsx`:
```tsx
export const metadata: Metadata = {
  icons: {
    icon: '/favicon.ico',
    apple: '/apple-touch-icon.png',
    shortcut: '/favicon.ico',
  },
  themeColor: '#02488D', // SPATIAL navy
  // ... existing title/description
}
```

### Phase 3 — Header Logo Integration
Update `src/components/shell/Header.tsx`:
- Replace "TNA SYSTEM" text with `<Image src="/brand/logo-icon.svg" ...>` 
- Add `<Image>` for wordmark (EN/AR variants) from `public/brand/` if available
- Keep existing breadcrumb/search/notification/avatar features

### Phase 4 — Sidebar Theme Consistency
Ensure `src/components/layout/RoleSidebar.tsx` applies the blue theme:
- Confirm gradient uses `--color-primary-gradient` (navy→cyan)
- Verify active states use SPATIAL tokens (`--color-primary`, `--color-info-bg`)
- Desktop sidebar background: `bg-surface-200` (as in AppShell)

### Phase 5 — Layout Consolidation
Decide on single layout system:
- **Option A (Preferred)**: Use `AppShell` as the sole wrapper (includes Header, Sidebar, BottomNav)
- **Option B**: Merge `AppShell` features into `AppLayout` and retire one component
- Verify all role pages (visitor/owner/gov/carrier) import the correct layout

### Phase 6 — Commit All Changes
Stage and commit the complete version as a single atomic commit:
```
feat: restore complete SPATIAL-branded header, sidebar, and assets
```
Include in commit:
- Favicon files
- Metadata updates
- Header logo integration
- Any token or style fixes needed for blue sidebar rendering
- Removal of duplicate/inline header code if consolidated

---

## Success Criteria
- [ ] Favicon appears in browser tab
- [ ] Header displays logo image (not just text)
- [ ] Sidebar shows blue gradient header block with role badge
- [ ] All brand assets (`public/brand/`) are tracked in Git
- [ ] No duplicate header implementations remain
- [ ] Build passes with zero errors

---

## Questions for User
1. **Layout choice**: Should we keep `AppShell` as the unified layout (recommended) or consolidate into `AppLayout`?
2. **Logo variants**: Use only the icon (`logo-icon.svg`) in header, or also include full wordmark (`logo-full-en.svg`/`logo-full-ar.svg`)?
3. **Favicon source**: Generate from existing `logo-icon.svg` manually, or use a tool/script to automate?
