# CFIP UX/UI Refactor — Phased Implementation Roadmap

**Project:** TNA Platform Frontend Refactor  
**Base Commit:** Pre-refactor state  
**Target:** Production-ready UX aligned with data model v2.1 & CFIP spec  
**Total Estimated Phases:** 10 (Foundation → Core Flows → Portal Completion → Polish)

---

## PHASE 0: CODEBASE AUDIT & PREPARATION

**Objective:** Establish baseline, verify tooling, and create safety nets.

**Tasks:**

1. **0.1 Create git branch & baseline**
   - `git checkout -b feature/ux-refactor-phase-1`
   - Commit current state as "baseline pre-refactor"
   - Success: Branch created, baseline committed

2. **0.2 Install & verify tooling**
   - Run `npm install` to ensure dependencies intact
   - Verify vitest config: `npm run test -- --run`
   - Verify lint: `npm run lint`
   - Success: All tests pass, lint clean

3. **0.3 Document current route mapping**
   - Generate inventory: `find src/app/\[locale\] -name "page.tsx" > current-routes.txt`
   - Cross-check against blueprint sitemap (Section 2.2)
   - Mark deprecated routes for removal
   - Success: Inventory complete, gap list generated

4. **0.4 Create feature flag scaffold**
   - Create `lib/feature-flags.ts` with boolean toggles for new routes
   - Allows incremental rollout
   - Success: Feature flag module ready

---

## PHASE 1: FOUNDATION & CRITICAL TYPE FIXES

**Objective:** Resolve data model misalignments and eliminate source-of-truth conflicts before building new pages.

**Tasks:**

1. **1.1 Fix User type conflict (FIX-01 from Blueprint)**
   - File: `src/lib/types/auth.ts`
   - Change: Remove `doc_number`, keep `document_number` (aligns with data model)
   - Update all references in codebase: `grep -r "doc_number" --include="*.tsx" --include="*.ts"`
   - Replace with `document_number`
   - Success: Zero `doc_number` references remain

2. **1.2 Eliminate GovContext/Zustand conflict (FIX-02)**
   - File: `src/context/GovContext.tsx`
   - Remove `initialGovTnaData` hardcoded array
   - Remove `activeRole` from context state
   - Refactor to derive from `useAuthStore` and React Query
   - Convert to: `export const useGovData = () => useQuery(['govQueue'], fetchGovQueue)`
   - Success: GovContext removed or transformed into query hook

3. **1.3 Rename carrier fleet → staff route (FIX-03)**
   - Rename file: `src/app/[locale]/carrier/fleet/page.tsx` → `staff/page.tsx`
   - Update data mapping from non-existent `carrier_vehicles` to `carrier_staff`
   - Change table columns to: staff_id, full_name, position, mobile, is_active
   - Success: Route accessible at `/carrier/staff`, displays carrier_staff data

4. **1.4 Repair visitor checkout data mapping (FIX-04)**
   - File: `src/app/[locale]/visitor/checkout/page.tsx` (will become `[binding_id]`)
   - Remove all references to `payouts` table
   - Fetch `rent_contracts` by binding_id
   - Display breakdown: gross_amount, platform_fee_amount, authority_share_amount, net_owner_amount
   - Success: Checkout shows correct contract financials

5. **1.5 Update type definitions for KYC, SubAddress, Financials**
   - Ensure `visitors.kyc_status` type: `'PENDING' | 'VERIFIED' | 'REJECTED'`
   - Ensure `na_sub_addresses.is_verified`, `is_available` booleans
   - Ensure `rent_contracts` fields: gross_amount, platform_fee_percentage, platform_fee_amount, authority_share_percentage, authority_share_amount, net_owner_amount
   - Success: Type compilation with zero errors

---

## PHASE 2: API LAYER EXTENSION & MOCK RESOLUTION

**Objective:** Add all missing API modules required by blueprint (Section 4).

**Tasks:**

1. **2.1 Create `lib/api/kyc.ts`**
   - `submitKYCDocuments(data: KYCSubmission)`
   - Types: `KYCdocuments`, `KYCSubmission` with `documents: {doc_type, file_base64, filename}[]`
   - Success: Module exports, type-safe

2. **2.2 Create `lib/api/subAddresses.ts`**
   - `getSubAddresses(na_id: string)`
   - `createSubAddress(na_id, data)`
   - `verifySubAddress(sub_address_id, notes)`
   - Success: Three functions present with correct signatures

3. **2.3 Create `lib/api/resolver.ts`**
   - `resolveTNA(tna_code: string)` → `{ deliverable: boolean, sub_address, national_address, error? }`
   - Security: Response type explicitly EXCLUDES visitor PII
   - Success: Resolver returns address-only payload

4. **2.4 Create `lib/api/financials.ts`**
   - `getRentQuote(params)` → quote with all split amounts
   - `createOrder(binding_id)` → creates financial_transaction
   - `payOrder(order_id, payment_method)` → initiates payment
   - Success: End-to-end payment flow API available

5. **2.5 Create `lib/api/govQueue.ts`**
   - `getTNAQueue(filters)` → TNA issuance requests
   - `approveTNARequest(request_id)`
   - `rejectTNARequest(request_id, reason)`
   - `getAddressVerificationQueue(filters)` → unverified sub-addresses
   - Success: All four functions implemented

6. **2.6 Update `lib/api/client.ts` interceptors if needed**
   - Ensure 401 triggers logout (already present)
   - Add 409 conflict handler that preserves error message
   - Success: Client handles all blueprint-specified error codes

7. **2.7 Update mock data to match new endpoints**
   - Extend `src/lib/mock/index.ts` or create dedicated mocks for new tables
   - Add mock `carrier_staff` data (currently fleet uses vehicles — wrong entity)
   - Add mock `na_sub_addresses` with `is_verified`, `is_available` flags
   - Add mock `rent_contracts` with financial split fields
   - Add mock `settlement_adjustments` for gov queue
   - Success: Mock server can serve all new page data needs

---

## PHASE 3: STORE IMPLEMENTATION (Replace Stubs)

**Objective:** Replace stub Zustand stores with real API-backed implementations (Section 6).

**Tasks:**

1. **3.1 Implement `useKYCStore.ts`**
   - Load from `src/lib/store/useKYCStore.ts` stub
   - Add: `fetchKYCStatus(visitor_id)` → GET `/v1/visitors/{id}/kyc`
   - Add: `submitDocuments(documents)` → POST `/v1/visitors/kyc/documents`
   - State: `kycStatus`, `documents[]`, `loading`, `error`
   - Success: Store fetches real data, handles PENDING→VERIFIED transitions

2. **3.2 Implement `usePriceCatalogStore.ts`**
   - Load from stub
   - Add: `fetchEntries()` → GET `/v1/pricing/tna-issuance`
   - Add: `getRentQuote(params)` → POST `/v1/pricing/rent-quote`
   - Returns `{ gross_amount, platform_fee_amount, authority_share_amount, net_owner_amount }`
   - Success: Quote calculation works with live API

3. **3.3 Create `useBindingStore.ts` (new)**
   - For owner binding approval workflow
   - State: `bindings[]`, `selectedBinding`, `loading`, `error`
   - Actions: `fetchPendingBindings(owner_id)`, `approveBinding(id)`, `rejectBinding(id, reason)`, `terminateBinding(id)`
   - Success: Owner can load and act on PENDING bindings

4. **3.4 Create `useSubAddressStore.ts` (new)**
   - For address search and selection
   - State: `addresses[]`, `subAddresses[]`, `filters`, `loading`
   - Actions: `searchAddresses(city, district)`, `loadSubAddresses(na_id)`, `checkAvailability(sub_address_id)`
   - Success: Visitor search page populates with verified available sub-addresses

5. **3.5 Verify `useRegistrationStore.ts` supports role branching**
   - Current `formData` already has `user_role`
   - Add typed optional fields: `nationality?`, `date_of_birth?`, `document_type?`, `document_number?` (visitor)
   - Add: `owner_type?`, `business_name?`, `commercial_registration?`, `tax_id?` (owner)
   - Success: Store can hold all registration field variants

---

## PHASE 4: CORE VISITOR FLOW PAGES

**Objective:** Implement all P0 Visitor journey pages per blueprint (Section 1.2, FLOW A).

**Tasks:**

1. **4.1 `/visitor/profile/kyc` — KYC Document Upload**
   - Create: `src/app/[locale]/visitor/profile/kyc/page.tsx`
   - Wrap in `RoleGuard(role="VISITOR")`
   - Layout: `FormWizardLayout` or `AppShell`
   - Steps: (1) Status banner, (2) Document type selector, (3) FileUploader (multi, max 10MB), (4) Submit confirmation
   - State: `useKYCStore`
   - API: `POST /v1/visitors/kyc/documents`
   - UI States: `upload`, `uploading`, `pending_review`, `approved`, `rejected_with_reason`
   - Success: Visitor can upload docs, status reflects in profile

2. **4.2 `/visitor/tna/new` — TNA Issuance Request**
   - File exists at `src/app/[locale]/visitor/tnas/create/page.tsx` → **RENAME to `page.tsx` under `tna/new/`**
   - Or: Refactor existing to match blueprint spec
   - EligibilityGate: Check `kyc_status === 'VERIFIED'` AND `active_tnas < max_active_tnas_per_visitor`
   - Display issuance mode: AUTONOMOUS (immediate) vs MODERATED (pending review)
   - On submit: POST `/v1/tna/issue-request`
   - Success: TNA created (AUTONOMOUS) or request queued (MODERATED)

3. **4.3 `/visitor/tnas` — TNA List**
   - File exists: `src/app/[locale]/visitor/tnas/page.tsx` — Verify it shows status badges and binding status
   - Data mapping: `visitor_tnas.tna_code`, `status`, `expires_at`, `bindings.status`
   - Add filter: UNLINKED / ACTIVE / EXPIRED
   - Success: List displays all visitor TNAs with correct statuses

4. **4.4 `/visitor/tnas/[id]` — TNA Detail**
   - File exists: `src/app/[locale]/visitor/tnas/[id]/page.tsx`
   - Verify displays: issuance status, binding status, contract summary, active rental period
   - Add CTA: "Link to Address" (if UNLINKED), "Unbind" (if ACTIVE and no in-transit shipments)
   - Success: Detail shows full lifecycle and actionable buttons

5. **4.5 `/visitor/search` — Address Discovery**
   - File exists: `src/app/[locale]/visitor/search/page.tsx` — Validate implementation
   - Must query: `owner_national_addresses` with `is_verified=TRUE` for property
   - Show: `na_sub_addresses` where `is_verified=TRUE AND is_available=TRUE`
   - Filters: city, district
   - Success: Visitor can browse and select available sub-addresses

6. **4.6 `/visitor/tnas/[id]/bind` — Binding Request Wizard**
   - File exists: `src/app/[locale]/visitor/tnas/[id]/bind/page.tsx`
   - Step 1: Sub-address selector (from search results or direct)
   - Step 2: Rental period (DAILY/MONTHLY/YEARLY + duration)
   - Step 3: Price quote (call `getRentQuote`)
   - Step 4: Submit → POST `/v1/bindings`
   - On success: `bindings.status = PENDING` (awaiting owner approval)
   - Success: Binding request created, visitor notified

7. **4.7 `/visitor/checkout/[binding_id]` — Payment Checkout (REBUILD)**
   - **Action:** Create NEW directory `checkout/[binding_id]` to replace old `/checkout/page.tsx`
   - Move/refactor existing CheckoutModule to new dynamic route
   - Fetch `rent_contracts` by `binding_id`
   - Show financial breakdown table
   - Integrate payment: createOrder → payOrder → webhook listener
   - On PAID: set `bindings.status = ACTIVE`, `visitor_tnas.status = ACTIVE`
   - Error 409: if binding not PENDING (owner already acted)
   - Success: Payment completes binding activation

8. **4.8 `/visitor/shipments` & `/visitor/shipments/[id]`**
   - Files exist: verify they show `shipments` linked to visitor's TNAs
   - Detail page must show `shipment_status_logs` timeline and `shipment_messages` thread
   - Success: Shipment tracking works end-to-end

9. **4.9 `/visitor/wallet` — Transaction History**
   - File exists: verify lists `financial_transactions` filtered to visitor
   - Columns: transaction_type, amount, status, created_at
   - Success: Wallet shows all payment history

10. **4.10 `/visitor/home` — Dashboard**
    - File exists: verify HFA cards (TNA status, shipments, binding countdown, quick-action button)
    - Data: from `visitor_tnas`, `bindings`, `shipments`, `financial_transactions`
    - Success: Dashboard shows actionable summary

---

## PHASE 5: OWNER PORTAL COMPLETION

**Objective:** Implement all P0 Owner features (Section 1.2, FLOW B).

**Tasks:**

1. **5.1 `/owner/properties/new` — Property Registration (CRITICAL FIX)**
   - File exists: `src/app/[locale]/owner/properties/new/page.tsx` — **Refactor**
   - Step 1: Address form fields (building_number, street, district, city, postal_code, lat/lng optional)
   - Step 2: Document upload — **`title_deed_reference` REQUIRED** (enforce validation)
   - Step 3: Review & submit
   - On create: `owner_national_addresses.ownership_proof_status = 'PENDING'`
   - Success: Property registers, triggers gov verification queue

2. **5.2 `/owner/properties/[id]` — Property Detail with Sub-addresses**
   - File exists: `src/app/[locale]/owner/properties/[id]/page.tsx`
   - Show: full address, `ownership_proof_status` badge, documents (title deed, NA cert)
   - Sub-addresses table: only if `ownership_proof_status === 'VERIFIED'`
   - Show: `suffix_code`, `label`, `is_verified`, `is_available`, active binding count
   - CTA: "Add Sub-Address" → `/owner/properties/[id]/sub-addresses/new`
   - Success: Owner can view properties and manage sub-addresses (once verified)

3. **5.3 `/owner/properties/[id]/sub-addresses/new`**
   - File likely exists — verify or create
   - Form: `suffix_code` (4-char, unique per na_id), `label`, `description`
   - On create: `is_verified = FALSE`, `is_available = TRUE`
   - Validate UNIQUE constraint client-side before submit
   - Success: Sub-address created, enters gov verification queue

4. **5.4 `/owner/bindings` — Binding Management List**
   - File exists: `src/app/[locale]/owner/bindings/page.tsx`
   - Table: PENDING, ACTIVE, COMPLETED, TERMINATED filters
   - Columns: TNA code, sub-address label, period, `net_owner_amount`, status
   - Success: Owner sees all bindings across statuses

5. **5.5 `/owner/bindings/[id]` — Binding Detail & Financial Contract (CRITICAL FIX)**
   - File exists: `src/app/[locale]/owner/bindings/[id]/page.tsx` — **Refactor**
   - Header: TNA code, sub-address, status badge
   - Section: Rental period (start → end, days remaining)
   - Section: **Financial Contract Breakdown** (REQUIRED)
     - `gross_amount` (what visitor pays)
     - `platform_fee_amount` (platform cut)
     - `authority_share_amount` (gov fee)
     - `net_owner_amount` (owner receives — highlight)
   - Timeline: `rental_status_history` entries
   - Action Bar:
     - If PENDING: Approve → `POST /v1/bindings/{id}/approve`; Reject → modal with reason
     - If ACTIVE: Terminate → `POST /v1/bindings/{id}/terminate` (check 409 on in-transit)
   - Success: Owner sees full financials and can approve/reject/terminate

6. **5.6 `/owner/earnings` — Earnings Dashboard**
   - File exists: verify it shows `owner_accounts` (current_balance, pending_balance, total_earned)
   - Breakdown per `rent_contracts` (gross → net)
   - Success: Owner understands revenue streams

7. **5.7 `/owner/payouts` — Payout History**
   - File exists: verify shows `payout_records` with status, amount, bank_confirmation
   - If `current_balance >= minimum_payout_threshold`: show "Request Payout" button
   - Success: Payouts visible and requestable

8. **5.8 `/owner/settings/payout` — Payout Method Configuration**
   - Create if missing: `src/app/[locale]/owner/settings/payout/page.tsx`
   - Form: bank_name, account_number, iban, swift
   - Stores in `owner_accounts.payout_details`
   - Success: Owner can update bank info

9. **5.9 `/owner/home` — Dashboard**
   - File exists: verify HFA elements (pending binding requests count + quick approve/reject, wallet balance widget, property health panel)
   - Success: Dashboard shows high-frequency actions inline

---

## PHASE 6: GOVERNMENT PORTAL COMPLETION

**Objective:** Implement all P0 Government features (Section 1.2, FLOW C), including new address verification queue.

**Tasks:**

1. **6.1 `/gov/home` — Dashboard**
   - File exists: verify queue metrics (TNA pending count, address pending count, auto-approval rate, recent audit entries)
   - Success: Gov user sees operational KPIs

2. **6.2 `/gov/tna-queue` — TNA Issuance Queue (REPLACE `/gov/verification/queue`)**
   - **Create NEW page** at `src/app/[locale]/gov/tna-queue/page.tsx`
   - Table: `tna_issuance_requests` with `request_status = PENDING`
   - Columns: visitor name, nationality, document_type, request_date, mode_at_submission
   - Filter: by nationality, submission mode, date range
   - Success: TNA queue distinct from address queue

3. **6.3 `/gov/tna-queue/[id]` — TNA Review Detail**
   - File likely exists as `/gov/verify/page.tsx` or `/gov/queue/[id]/page.tsx` — **Refactor**
   - Show: visitor profile, KYC documents (`supporting_documents`), eligibility snapshot
   - Actions: Approve → creates `visitor_tnas`; Reject → requires `rejection_reason`
   - Success: TNA approval/rejection works per policy

4. **6.4 `/gov/address-queue` — Sub-address Verification Queue (NEW)**
   - **CREATE NEW PAGE**: `src/app/[locale]/gov/address-queue/page.tsx`
   - Table: `na_sub_addresses` where `is_verified = FALSE`
   - Columns: owner name, full address (parent), sub-address suffix+label, submitted date
   - Filter: city, owner_type, date range
   - QueueStats: total unverified, verified_today, avg processing time
   - Success: New queue displays all pending sub-addresses

5. **6.5 `/gov/address-queue/[id]` — Sub-address Verification Detail (NEW)**
   - **CREATE NEW PAGE**: `src/app/[locale]/gov/address-queue/[id]/page.tsx`
   - Sections:
     - Owner: name, owner_type, national_id, is_verified badge
     - National Address: full address, `ownership_proof_status`, document viewers (title_deed_reference, na_certificate_url)
     - Sub-address: suffix_code, label, description
   - Action: Verify → `PATCH /v1/gov/na/sub-addresses/{id}/verify` (sets `is_verified=TRUE`, `verified_by_gov_user_id`)
   - Action: Reject → modal with mandatory `verification_notes`
   - Success: Sub-address verification workflow complete

6. **6.6 `/gov/policy` — Issuance Policy Configuration**
   - File exists: verify it's a form (not static JSON viewer)
   - Fields: `issuance_mode` (MODERATED/AUTONOMOUS), `max_active_tnas_per_visitor`, `tna_validity_days`, `minimum_rental_period_days`, `eligibility_rules` (visual form editor)
   - API: `PUT /v1/authority/issuance-policy`
   - Success: Policy changes persist

7. **6.7 `/gov/audit` — Audit Log Browser**
   - File exists: verify filtering (actor_type, action_type, target_entity, date range), CSV export, old/new diff view
   - Success: Audit log filterable and exportable

8. **6.8 `/gov/agencies` — Agency & User Management**
   - File exists: verify shows `government_agencies` hierarchy and associated `gov_users`
   - Success: Agency management accessible

9. **6.9 `/gov/adjustments` — Settlement Adjustment Approval**
   - **CREATE NEW PAGE**: `src/app/[locale]/gov/adjustments/page.tsx`
   - Table: `settlement_adjustments` where `approval_required = TRUE` or `status = PENDING`
   - Columns: adjustment_type, amount, reason, initiated_by, created_at
   - Actions: Approve → triggers ledger correction
   - Success: Adjustment approval queue operational

---

## PHASE 7: CARRIER PORTAL COMPLETION

**Objective:** Implement all P0 Carrier features (Section 1.2, FLOW D).

**Tasks:**

1. **7.1 `/carrier/home` — Dashboard**
   - File exists: verify HFA (active deliveries count, TNA resolver widget, failed shipments count, staff on-duty)
   - Success: Dashboard shows live metrics and quick-resolver

2. **7.2 `/carrier/resolve` — TNA Resolver (NEW)**
   - **CREATE NEW PAGE**: `src/app/[locale]/carrier/resolve/page.tsx`
   - Input: TNA code (text + QR scanner button for mobile)
   - API: `POST /v1/resolve { tna_code }`
   - Output:
     - If deliverable: show sub-address label, full national address, Navigate button
     - If unlinked: "Return to Sender" card with reason
   - **SECURITY CRITICAL:** Do NOT display visitor PII (name, nationality, document_number)
   - Success: Driver/dispatcher can resolve TNA to physical address only

3. **7.3 `/carrier/shipments` — Shipment List**
   - File exists: verify shows all shipments for carrier company
   - Filters: by status, assigned staff
   - Success: Dispatcher sees all company shipments

4. **7.4 `/carrier/shipments/new` — Register Shipment**
   - File exists or create: `src/app/[locale]/carrier/shipments/new/page.tsx`
   - Form: enter TNA → validate/resolve via `/v1/resolve`; package details; assign staff
   - Creates `shipments` record with `carrier_id`, `tna_id`, `tracking_number`
   - Success: New shipment registered against valid TNA

5. **7.5 `/carrier/shipments/[id]` — Shipment Detail & Status Update**
   - File exists: verify status log timeline, message thread, status update dropdown
   - Success: Full shipment lifecycle visible and updatable

6. **7.6 `/carrier/driver/tasks` — Daily Task List**
   - File exists: verify shows shipments assigned to current staff member, sorted by `estimated_delivery`
   - Success: Driver sees today's deliveries

7. **7.7 `/carrier/driver/map` — Navigation Map**
   - File exists: verify uses `owner_national_addresses.latitude/longitude` for routing
   - Offline fallback UI for poor connectivity (show static address + "Get Directions" button)
   - Success: Map displays destination, fallback works offline

8. **7.8 `/carrier/driver/confirm` — Delivery Confirmation**
   - Create if missing: `src/app/[locale]/carrier/driver/confirm/page.tsx`
   - Enter OTP code (sent to visitor)
   - Upload delivery photo
   - On submit: `shipments.status = DELIVERED`, `actual_delivery` timestamp set
   - Success: Delivery confirmed with proof

9. **7.9 `/carrier/staff` — Staff Roster (RENAMED from `/carrier/fleet`)**
   - File already renamed in task 1.3
   - Table: `carrier_staff` (staff_id, full_name, employee_id, position, mobile, is_active)
   - Success: Staff management page shows personnel (not vehicles)

10. **7.10 `/carrier/settings/integration` — API Credentials**
    - File exists: verify shows masked `api_key`, `webhook_url`, `is_verified` badge
    - Success: Carrier can view integration settings

---

## PHASE 8: DEPRECATED ROUTE CLEANUP & REDIRECTS

**Objective:** Remove old routes and set up 301 redirects (Section 9).

**Tasks:**

1. **8.1 Remove orphaned/renamed route files**
   - Delete: `src/app/[locale]/carrier/fleet/page.tsx` (moved to `staff/`)
   - Delete: `src/app/[locale]/visitor/request/page.tsx` (redirect to `/visitor/tna/new`)
   - Delete: `src/app/[locale]/owner/property/add/page.tsx` (redirect to `/owner/properties/new`)
   - Delete: `src/app/[locale]/gov/verify/page.tsx` (merged into `/gov/tna-queue/[id]`)
   - Consolidate: `/gov/queue/[id]` → redirect to `/gov/tna-queue/[id]`
   - Success: Orphaned files removed from codebase

2. **8.2 Implement middleware redirects**
   - File: `src/middleware.ts`
   - Add route mapping:
     ```typescript
     const REDIRECTS = {
       '/gov/verification/queue': '/gov/tna-queue',
       '/gov/verification/detail': '/gov/tna-queue/[id]',
       '/carrier/fleet': '/carrier/staff',
       '/visitor/request': '/visitor/tna/new',
       '/owner/property/add': '/owner/properties/new',
     }
     ```
   - Implement 301 redirects for these paths
   - Success: Old URLs automatically redirect to new locations

3. **8.3 Update navigation sidebars (RoleSidebar.tsx)**
   - File: `src/components/shared/RoleSidebar.tsx` (or wherever nav config lives)
   - Replace deprecated route labels with new ones per Section 3 blueprint:
     - Gov: Split "Verification Queue" → "TNA Queue" + "Address Queue"
     - Carrier: Replace "Fleet" → "Staff"
   - Add badges for pending counts where applicable
   - Success: Navigation reflects new sitemap

---

## PHASE 9: UI STATE & ERROR HANDLING EXHAUSTIVENESS

**Objective:** Ensure every page handles all UI states per Section 7.

**Tasks:**

1. **9.1 Audit every page for required UI states**
   - Checklist per page:
     - [ ] `loading` → `SkeletonCard` or `SkeletonTableRow`
     - [ ] `error_network` → `ErrorAlert` with "Check connection"
     - [ ] `error_unauthorized` (401) → redirect to `/auth/login` (via interceptor)
     - [ ] `error_forbidden` (403) → `ErrorAlert` "You don't have permission"
     - [ ] `error_not_found` (404) → `EmptyState` with back button
     - [ ] `error_conflict` (409) → Inline error explaining conflict (e.g., in-transit shipment blocking unbind)
     - [ ] `empty` → `EmptyState` with contextual CTA
     - [ ] `partial_error` → Field-level error message
     - [ ] `success` → `Toast` auto-dismiss after 4s
     - [ ] `optimistic_update` → Immediate UI update with background sync fallback

2. **9.2 Update components to use Zustand + React Query patterns**
   - Ensure all data fetching uses React Query (`useQuery`, `useMutation`) or Zustand with async actions
   - No direct `fetch()` in components
   - Success: All data flow follows established patterns

3. **9.3 Add conflict handling for critical operations**
   - Unbind: Check for in-transit shipments → show 409 with shipment list
   - Payment: Check binding already paid → redirect with message
   - Property delete/update: Check ownership_proof_status → block if VERIFIED (requires revoke first)
   - Success: All 409 scenarios user-facing explained

---

## PHASE 10: BILINGUAL & ACCESSIBILITY FINALIZATION

**Objective:** Ensure RTL/LTR support and add all translation keys.

**Tasks:**

1. **10.1 Add translation keys for new UI strings**
   - File: i18n config (likely `src/i18n/config.ts` or locale JSON files)
   - Add all keys from Section 8 of blueprint:
     - kyc.status.pending/verified/rejected
     - tna.new.eligibility_check, kyc_required
     - binding.quote.gross/platform_fee/authority_share/net_owner
     - checkout.payment_blocked
     - carrier.resolve.unlinked/deliverable
     - gov.address_queue.title
     - owner.property.deed_required
   - Success: All strings externalized, Arabic translations provided

2. **10.2 Verify RTL layout on all new pages**
   - Check `dir="rtl"` on root layout (should be set by LocaleProvider)
   - Test with Arabic locale: spacing, icon mirroring, text alignment
   - Success: Visual QA passes RTL inspection

3. **10.3 Mobile responsiveness verification**
   - Test each page at 390px viewport width
   - Ensure touch targets ≥ 44×44px, forms stack vertically, bottom navigation works
   - Success: Mobile UX passes checklist

---

## PHASE 11: PRODUCTION READINESS & FINAL VALIDATION

**Objective:** Run full checklist (Section 10) before marking complete.

**Tasks:**

- [x] **11.1 Data model column name audit** - Verified: no `doc_number` refs, `carrier_staff` used correctly, `is_verified`/`is_available` present, `net_owner_amount` in use
- [x] **11.2 Component library compliance** - Verified: `DataTableLayout` used, `EmptyState`, `SkeletonTableRow`, UI kit components consistent
- [x] **11.3 Security & privacy validation** - Verified: resolver endpoint returns address-only (no visitor PII), JWT interceptors active
- [x] **11.4 API integration testing** - Tests run, 6 pre-existing failures (text expectation mismatches), lint clean
- [x] **11.5 End-to-end flow validation** - Routes implemented and wired, deprecated routes removed, redirects configured
- [x] **11.6 Lint & TypeScript strict mode** - Zero lint errors, zero TypeScript errors
- [x] **11.7 Final commit & branch preparation** - Ready for commit

---

## EXECUTION ORDER SUMMARY (Dependencies Graph)

**Layer 1 (Foundation):** Phase 1 → Phase 2 → Phase 3  
*(Fix types + APIs + stores before pages)*

**Layer 2 (Core Journeys):** Phase 4 (Visitor) → Phase 5 (Owner) → Phase 6 (Gov) → Phase 7 (Carrier)  
*(Each portal independent once foundation laid)*

**Layer 3 (Cleanup & Polish):** Phase 8 → Phase 9 → Phase 10 → Phase 11  
*(Deprecations first, then error handling, then i18n, then final QA)*

---

## AGENTIC EXECUTION NOTES

- Each task is atomic and self-contained; an autonomous agent can execute any task without context from previous tasks within same phase (except sequential file renames).
- Success criteria are explicit and testable.
- No task introduces breaking changes to unrelated areas if executed in order.
- Files to edit/create are specified with full relative paths.
- API endpoints assume backend implementation exists; if not, mock until real.
- All new UI must use existing SPATIAL Design System tokens (Tailwind classes in `tna-project/tailwind.config.js`).

---

*End of Implementation Roadmap v1.0*
