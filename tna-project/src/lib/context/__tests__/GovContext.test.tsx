import React from 'react';
import { render, screen } from '@testing-library/react';
import { describe, it, expect } from 'vitest';

describe('GovContext', () => {
    it.skip('requires GovContext implementation', () => {
        expect(true).toBe(true);
    });
});